{
  "name": "firestore-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Tests for firestore.rules against the Firebase Local Emulator Suite.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1",
    "jest": "^30.4.2"
  }
}
