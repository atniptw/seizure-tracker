*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties
*.apk
*.aab
app/google-services.json
app/debug.keystore
# Signing material / release config — never commit (a leaked keystore password
# can't be un-leaked by a later commit; a lost release keystore permanently
# orphans the Play Store listing). Keep backups outside this repo.
*.jks
*.keystore
key.properties
*.p12
node_modules/
firestore-debug.log
firebase-debug.log
ui-debug.log

# Claude dev-team — ephemeral merge-gate markers (see planning/claude-dev-team.md).
# The team journal .claude/team/log/ IS committed; only these two are local state.
.claude/team/review-verdict.md
.claude/team/last-green
