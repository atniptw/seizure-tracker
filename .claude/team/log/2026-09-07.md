
## 18:18 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** CI green on `ca955e1` (items 11–14). --- ## Item 13 (still open) + Items 15–17 (Review history) **Item 13 — health note form.** Still unresolved: it has two text fields ("description" + "notes") on a form whose whole point is minimalism. Collapse to one, or keep both? (Quick yes/no.) **Item 15 — Dashboard.** *"days since last seizure, total count, recent entries."* Fine, scoped to the active pet. Minor: "total count" — of **seizures** specifically (not all entries), yes? And trend chart / all-pets view stay deferred. **Item 16 — Full history — contradiction.** The bullet says *"every entry, most recent first, **grouped by month**"* but §4.0's deferred table lists *"month grouping"* as later, and `flutter-migration.md` says parity = **flat list**. So the next release is a **flat list, most recent first, active pet only** — I'll fix the bullet to match (drop "grouped by month"). Agreed? **

## 18:23 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** CI green on `ca955e1` (items 11–14). --- ## Items 18 & 19 — Share with the vet I checked the shipped `ExportScreen` / `ExportViewModel`. **Item 18 — the two share paths.** Solid. One wording nit: the bullet says *"walk through the dashboard, **charts**, or timeline"* — the trend chart is deferred (§4.0), so it's "dashboard or timeline" for the next release. **Item 19 — export options.** - **Date range** — shipped has Last 30 / Last 90 / All time / **Custom** (start+end). Spec only lists "30/90/all" — I'll add "custom" to match. - **Pet selection** — shipped is **one pet OR "all pets"**, not arbitrary multi-select. The spec says "which pet(s)" which oversells it. I'll reword to "one pet, or all pets." - **⚠️ archived pets break the item-3 promise.** I told you (item 3) that "an archived pet's history still appears in an all-time export." But the export's pet list comes from the same sourc

## 18:26 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** CI green on `555f4be` (items 13, 15–17); `3ae8689` (18–19) still running. --- ## §4 feature-scope review — complete, all 19 items 12 commits on `main` (`5c4152b`..`3ae8689`), docs only. What changed: ### Scope cuts (moved to §4.0 backlog) | Item | Decision | |---|---| | **Anonymous sign-in** + all its safety machinery (link gates, nudges, stranded-identity handling) | Deferred as one unit — **next release is Google-only** | | **Join preview** ("you're about to join X") + QR join | Deferred; `codeIndex` stays `{ householdId }`-only | | **Medication "set an alarm" hand-off** + past-medications view | Deferred (flagged: no iOS system-alarm API) | | Photo/video, notifications, Apple sign-in, trend chart, all-pets view, history filters + month grouping, compare-to-similar, code rotation | Already deferred — confirmed | ### Pulled *into* the next release - **Pet archive** — was deferred; now i

## 18:33 UTC — subagent
**Task:** <local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat> 

**Result:** **Not yet — there's a required phase in front of it, and that phase hasn't started.** ## The sequencing `flutter-migration.md §2` is explicit: the backend/data-model move (`migration.md`) runs on the *current Kotlin app* first, gets verified on both phones, and only then does the Flutter build begin. Rewriting Flutter against the old Firestore shape means migrating data twice — that's the thing the ordering avoids. ## Where we actually are **`migration.md` — Phase 1, not started:** - Still marked *"draft for discussion"* (last touched 2026-08-30) — no sign-off from you. - No `tools/migrate/` (backup.js / restore.js / migrate.js don't exist). - No `firestore.indexes.json` (migration.md §4 area 3 flags this as a pre-window requirement if any server-side filter is added). - Models are still `Seizure.kt` + `HealthNote.kt` — the `Observation` envelope, `members/{uid}.role`, `private/config` j
