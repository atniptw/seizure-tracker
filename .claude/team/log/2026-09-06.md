
## 21:53 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** CI green on `c495974` (item 5). Rebased past a teammate's push (the Claude dev-team scaffolding — `c1b72d6`; my doc pushes are exempt from its new merge gate). Updated the stale `v2-team-shape` memory. --- ## Item 6 — Medications > *"Medications: add/edit/remove. No in-app reminders or dose-tracking — a 'set an alarm' action hands off to the phone's own alarm/reminder app…"* I checked the code. Two things aren't what the spec implies: 1. **"remove" should be "discontinue" — same call we just made for pets.** `migration.md` / `architecture.md` say discontinuing a med sets `active: false` + `endDate` (**not** a delete), to preserve history a vet asks about ("was he ever on X?"). But this product-spec bullet says "add/edit/**remove**". Same fix as pet archive: discontinue keeps the record; true delete only for a never-real entry. Want me to align the wording? 2. **The "set an alarm" hand-of

## 21:54 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** CI green on `c495974` (item 5). Rebased past a teammate's push (the Claude dev-team scaffolding — `c1b72d6`; my doc pushes are exempt from its new merge gate). Updated the stale `v2-team-shape` memory. --- ## Item 6 — Medications > *"Medications: add/edit/remove. No in-app reminders or dose-tracking — a 'set an alarm' action hands off to the phone's own alarm/reminder app…"* I checked the code. Two things aren't what the spec implies: 1. **"remove" should be "discontinue" — same call we just made for pets.** `migration.md` / `architecture.md` say discontinuing a med sets `active: false` + `endDate` (**not** a delete), to preserve history a vet asks about ("was he ever on X?"). But this product-spec bullet says "add/edit/**remove**". Same fix as pet archive: discontinue keeps the record; true delete only for a never-real entry. Want me to align the wording? 2. **The "set an alarm" hand-of

## 21:58 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** CI green on `02d7d1f` (item 6). --- ## Items 7 & 8 — Vet directory + pet↔vet links > *7: "One shared vet directory per household, not one list per pet."* > *8: "Link a vet to a pet with a role label; a pet can have several vets, a vet can serve several pets; edit/remove links."* I checked the code — the shipped app already does both cleanly and the spec matches. Notably: - `Vet` = name / phone / `addressOrNotes` (one free-text field). `PetVetLink` = flat auto-id doc, role on the link. - **`deleteVet` already cascades** — one `WriteBatch` deletes the vet's links + the vet doc. Its own code comment notes entries don't reference vets, so hard-delete loses no history. So vets **hard-delete** (unlike pets/meds which archive) — and that's correct, a vet contact has no history worth keeping. Genuinely open, all minor: 1. **Structured contact fields (email, split address)** — §3 already says "la

## 22:01 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** CI green on `02d7d1f` (item 6). --- ## Items 7 & 8 — Vet directory + pet↔vet links > *7: "One shared vet directory per household, not one list per pet."* > *8: "Link a vet to a pet with a role label; a pet can have several vets, a vet can serve several pets; edit/remove links."* I checked the code — the shipped app already does both cleanly and the spec matches. Notably: - `Vet` = name / phone / `addressOrNotes` (one free-text field). `PetVetLink` = flat auto-id doc, role on the link. - **`deleteVet` already cascades** — one `WriteBatch` deletes the vet's links + the vet doc. Its own code comment notes entries don't reference vets, so hard-delete loses no history. So vets **hard-delete** (unlike pets/meds which archive) — and that's correct, a vet contact has no history worth keeping. Genuinely open, all minor: 1. **Structured contact fields (email, split address)** — §3 already says "la

## 22:02 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** Holding for your answers on items 7 & 8 (structured contact fields stay deferred? add the "one link per pet–vet pair" sentence? tap-to-call in or out?). 

## 22:36 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** CI green on `fbfd3d4` (items 7-8). (Note: a SubagentStop hook from the new team scaffolding is auto-appending my turn summaries to the committed `.claude/team/log/` journal — that's why it rides along in commits. Working as designed, just noisy.) --- ## Items 9 & 10 — Manage household (admin) / member list (non-admin) > *9: "Admins: add a new member …, view the member list, remove a member, rename the household, view/rotate/share the join code."* > *10: "Non-admin members: view the member list only."* **Gap 1 — promote/demote to admin is missing from the spec entirely.** `security-privacy.md §4.4` fully specifies it (an admin writes the `role` field on another member's doc; needs the rules change that `migration.md` area 1 ships), and it's **in the next release**. But §4 "Manage household" never mentions it. This is load-bearing: the household starts with one admin (the creator), and §2 

## 22:38 UTC — subagent
**Task:** Read the Produc Spec. I'm reviewint it and want you to make changes 

**Result:** That's already what landed in `fbfd3d4` — §3 now reads: *"a single pet–vet pair may carry more than one link (the same clinic tagged both General and Emergency for one pet is fine; the link UI must not enforce one role per pair)."* Plus a matching note on the `LinkVetSheet` row in `flutter-migration.md`. So a vet linked to a pet can hold multiple roles. Nothing more to do there. Back to **items 9 & 10** — still need your call on the rewrite I proposed: - **Admins:** view roster; **promote/demote admin**; remove another member; leave themselves; rename household; view/share join code; can't remove/demote the last admin. Rotation later. - **Non-admins:** view roster (names + who's admin); can't promote, remove others, rename, or see the code; anyone can leave. The key addition is **promote/demote to admin**, which the spec currently omits entirely despite it being in the next release and t
